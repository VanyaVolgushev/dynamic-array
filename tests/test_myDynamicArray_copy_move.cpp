#include <gtest/gtest.h>
#include "MyDynamicArray.h"

TEST(MyDynamicArrayTest, CopyConstructor) {
    MyDynamicArray<int> arr;
    EXPECT_EQ(arr.size(), 0);
    EXPECT_EQ(arr.capacity(), 16);
}

TEST(MyDynamicArrayTest, CopyConstructor) {
    MyDynamicArray<int> arr;
    EXPECT_EQ(arr.size(), 0);
    EXPECT_EQ(arr.capacity(), 16);
}

TEST(MyDynamicArrayTest, CopyConstructor) {
    MyDynamicArray<int> arr;
    EXPECT_EQ(arr.size(), 0);
    EXPECT_EQ(arr.capacity(), 16);
}
